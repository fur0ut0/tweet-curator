# frozen_string_literal: true

ruby '3.0.0'

source 'https://rubygems.org'

gem 'oauth'
gem 'redis'

group :development, :test do
   gem 'dotenv'
   gem 'overcommit'
   gem 'pry'
   gem 'solargraph'
end
